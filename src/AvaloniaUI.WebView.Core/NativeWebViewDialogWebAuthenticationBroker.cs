using System;
using System.Threading.Tasks;

namespace AvaloniaUI.WebView;

internal static class NativeWebViewDialogWebAuthenticationBroker
{
    public static Task<WebAuthenticationResult> AuthenticateAsync(WebAuthenticatorOptions options)
    {
        throw new Exception();
    }
}
